name: Checks

on:
  push:
    branches:
      - development
  pull_request:
    branches:
      - development

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: Screenly/edge-apps-actions/checks@v1
